mkdir project_name
git init (if needed)
git add .
git commit -m "project starting files"
hub create
git push origin master

npm init
npm i express body-parser
(npm i ejs)

nodemon app.js

git add .
git commit -m "first commit"

hub create
git push origin master

heroku login
heroku create
git push heroku master

/after cloning a project that contains dependencies, e.g. modules that you don't have locally yet
npm install
